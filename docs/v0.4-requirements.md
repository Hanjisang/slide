# v0.4 需求状态

- **R2.1 PARTIAL（DEMO）**：规范版本、字段、映射元数据和规范关联生成已实现；现有 Exporter 仍使用标准数据结构，复杂字段映射/文件名模板待下一迭代接入。`DEMO_LOCAL_PATHOLOGY_V1` 明确为非正式规范。
- **R2.2 DONE**：Plan/Job 分离、优先级、幂等键、调度、执行记录和重试配置已实现；旧计划路径兼容。
- **R2.3 DONE**：预审核规则/问题/覆盖审计/重新审核和后端发送硬拦截已实现。
- **R2.4 PARTIAL**：Chunk 持久化、Mock Receiver、服务端进度对账、SHA-256、恢复 API、TLS/mTLS 配置已实现；正式平台协议和真实生产 HTTPS 验证为外部依赖。

外部依赖：正式字段字典、正式接口地址、正式鉴权/签名算法、证书和卫健委断点协议。
