# v0.2.0 验收记录

验收日期：2026-08-18
分支：`feature/v0.2-upgrade`

## 构建与运行

| 项目 | 实际结果 |
|---|---|
| Maven | `mvn -q test` 通过，7 个测试类 |
| Frontend | `vue-tsc --noEmit` 与 Vite production build 通过 |
| Docker | Compose 六服务运行；backend/MySQL/MinIO/worker healthy |
| 数据迁移 | 既有 v0.1 持久卷启动成功，日志显示 schema ready for v0.2.0 |
| 浏览器 | 桌面与 390 x 844，无横向溢出、重叠或 Vue 运行错误 |

## 场景证据

1. 切片管理：上传 `CMU-1-Small-Region.svs`（1,938,955 bytes），状态 `READY / AVAILABLE / OPENSLIDE`，元数据 `2220 x 2967`；thumbnail 54,838 bytes、tile 3,673 bytes，均为 JPEG。下载 MD5 `1ad6e35c9d17e4d85fb7e3143b328efe`，逻辑重命名成功。
2. 真正归档：HOT→`pathology-archive`，任务 `SUCCESS / 100%`。MinIO 实际对象大小 1,938,955，ETag/MD5 与源一致；源对象未删除。
3. 权限：VIEWER 的切片列表为 HTTP 200；切片删除、归档、用户管理均为 HTTP 403。OPERATOR 已完成上传、归档、备份和上报。
4. 数据校验：重复 `CASE000001` 返回 HTTP 400 `病理号重复` 并生成 UNIQUE 异常；入院时间晚于出院时间生成 CROSS_FIELD 异常，修正后待处理数为 0。
5. 文件版本：文件资产 V1 与 V2 的 object key 不同，两个 HOT 对象同时存在且 MD5 一致，不覆盖旧版本。
6. 备份：HOT→`pathology-backup`，任务 `SUCCESS / 100%`，源/目标 MD5 一致；MinIO 实际对象存在。
7. 病理一键上报：批次 ZIP 为 3,553,922 bytes，实际打开后含 `manifest.json`、`data/patient.json`、`data/pathology.json`、`data/diagnosis.json`、`data/visit.json` 和两张 `slides/*.svs`；生成物登记为 REPORT 文件资产。
8. 上报失败：Mock HTTP 500 后批次 `FAILED`、retry_count=1、next_retry_time=1 分钟并产生 WARNING 告警；调度器实际执行后 retry_count=2、下次为 5 分钟；恢复 HTTP 200 后人工上报 `SUCCESS`。代码的第三次退避为 30 分钟，第四次失败终止。
9. 监控：CPU、内存、磁盘、JVM Heap 均返回运行时百分比；三个 storage_target 返回对象数量/使用量；采集、切片、归档、备份、上报五类任务均返回真实状态统计。
10. 基础数据：SPECIMEN_TYPE CSV 和 SLIDE_FORMAT XLSX 均完成导出→导入，结果 1 成功/0 错误；CSV BOM 往返有自动测试。
11. 上报计划：PATHOLOGY 模板开启 include_slide 后生成 `PATHOLOGY_PACKAGE / ZIP`；MANUAL 计划执行生成并发送成功，last_run_time 已更新。
12. 逻辑删除：删除后列表无该切片、详情返回已删除，但 HOT MinIO 源对象仍存在。

## 自动测试覆盖

- `ArchiveServiceTest`：复制、存在性、大小/MD5、任务成功、源文件不删除。
- `BackupServiceTest`：BACKUP 等级、复制和 MD5 成功。
- `SlideFileServiceTest`：deleted/deleted_at/deleted_by 逻辑删除 SQL。
- `AuthInterceptorTest`：已认证但无权限返回 403。
- `PathologyPackageExporterTest`：manifest、四类数据 JSON 和实际切片字节。
- `BasicDataControllerTest`：带 UTF-8 BOM 的导出 CSV 可重新导入。
- `ReportExporterTest`：JSON/XML/CSV/通用 ZIP 格式。

## 未纳入 DONE

- 10 种厂商格式真实解析：缺少商业 SDK，标记 `EXTERNAL_DEPENDENCY / SDK_REQUIRED`。
- 正式卫健委接口：缺协议、签名/证书与生产接收地址；仅验证可配置 HTTP/File Sender 和 Mock 接收端。
