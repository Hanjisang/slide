# v0.4 验收记录

- Backend：`mvn -q test` 通过（含既有回归测试）。
- Frontend：`npm run build` 通过。
- 规范/Job/预审核/端点/传输 Tab 已接入现有数据上报页。
- 编译验证：`mvn -q -DskipTests compile` 通过。
- Docker/E2E 需要可用的 Docker Desktop、MySQL 和真实样本环境；当前代码提供 Mock Receiver API，不能把 Mock 结果标成正式卫健委接口验收。
