# 规范中心

`report_spec` 以 `spec_code + version` 唯一，保留历史版本并支持有效期、启停、格式、编码、压缩和白名单文件名元数据。`report_spec_field.source_expression` 仅保存受控字段路径，不能执行 SQL、脚本或用户代码。

内置 `DEMO_LOCAL_PATHOLOGY_V1` 只用于验收，不代表任何卫健委正式规范。接入正式规范时应新增版本，不覆盖历史记录。
